# Lightweight dependencies for Notebooks 00 and 01
# Install time: ~30 seconds (vs 5-10 minutes for full requirements)
#
# These notebooks focus on Python fundamentals and patient data structures,
# not AI inference. Heavy ML dependencies (transformers, torch, llama-cpp)
# are deferred until Notebook 02.

# Core data handling and visualization
matplotlib>=3.5.0
Pillow>=9.0.0
requests>=2.28.0

# Notebook environment
ipykernel>=6.0.0
ipywidgets>=8.0.0

# Typing support for patient data structures
typing-extensions>=4.0.0
